ЗАПУСК ПРОЕКТА 

проставляем нужные поля в .env (будет поднята бд, поэтому проставим для подключения локального) 

прописываем в терминале: docker compose up -d для запуска 2 контейнеров (приложения и бд (постгрес)) 

далее можно кидать запрос по хосту http://localhost:8080 (можно в енвишке указывать нцжный порт)